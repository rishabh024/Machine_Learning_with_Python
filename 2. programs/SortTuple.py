# sort the tuple in one line-

print(tuple(sorted(list(tuple([int(i) for i in input("enter all space separated values-").split()])))))

# a = tuple([int(i) for i in input("enter all space separated values-").split()])
# b = list(a)
# b.sort()
# print("sorted tuple is-", tuple(b))
